import { reactive } from "vue";

import { NodeManager } from "./nodes/NodeManager";

export const WorkspaceState = reactive({

    // =====================================================
    // Camera
    // =====================================================

    x: 0,

    y: 0,

    zoom: 1,

    // =====================================================
    // Current World
    // =====================================================

    currentWorld: "overview",

    // =====================================================
    // Selected Node
    // =====================================================

    selectedNode: null as string | null,

    // =====================================================
    // Workspace
    // =====================================================

    nodes: new NodeManager(),

    // =====================================================
    // UI
    // =====================================================

    inspectorOpen: true,

    sidebarOpen: true

});
